# Authority Stage 0 intake

The user authorized only Stage 0 from the sealed authority ceremony design:
pure canonicalization and independent fixed-vector fixtures. This run may add an
isolated downstream package and tests. It may not modify the live authority
candidate, create a service or database, change OS permissions, access hardware,
contact providers, or create/use any real key.

The deterministic signing key is an openly published test-only scalar derived
from a fixed label. It is cryptographic fixture material, never an identity or
secret.
