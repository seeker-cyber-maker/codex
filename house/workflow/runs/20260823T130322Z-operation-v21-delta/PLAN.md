# Operation contract v2.1 delta — plan v1

## Classification and routing advisory

- Project: existing Dream House repository.
- Recovery disposition: resume from council disposition `REVISE_DESIGN` at
  `3632bb49fc`.
- Case type: `systems_critical` design correction; no implementation.
- Recommendation: Sol / high for the design delta and root review.
- Replacement reviewer: OpenRouter explicit-free Nemotron, with one explicit
  free Cohere fallback, acting as adversarial methodologist.
- Reassess: after the replacement review disposition.

## Objective

Correct the five decision-bearing gaps in the v2 proposal, then ask one new
bounded adversarial lane whether any authority conflation or hidden I/O remains
before implementation.

## Authority and budget

- The design packet is `cloud-ok` and contains no credentials or raw account
  identifiers.
- Existing explicit-free provider route only; zero-cost model suffix required;
  no purchase or provider configuration change.
- One reviewer, one primary, one transport fallback, 90 seconds per attempt.
- Reviewer output is advisory and cannot modify files, grant authority, create
  a task, or dispatch a worker.
- No operation/controller mutation, credential projection, provider worker
  call, subprocess, runtime profile, lease, intent, or hardware action.

## Acceptance

- V2.1 is a standalone corrected contract, not prose that silently edits the
  sealed v2 packet.
- The delta explicitly closes each item in the prior synthesis.
- The reviewer receives the v2.1 contract, delta, and prior claim ledger under
  one transport hash.
- Root synthesis ends in `ACCEPT_V2_1`, `REVISE_V2_1`, or `BLOCKED` and names
  the smallest next action.
