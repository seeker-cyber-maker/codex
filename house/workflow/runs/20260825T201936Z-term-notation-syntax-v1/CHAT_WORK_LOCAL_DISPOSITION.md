# Chat/Work local disposition

## Transport result

The supervised `application.chat_work` Pi chair completed one print-only call
with no model-facing tools and no saved Pi session. Transport success proves a
response from the configured application lane, not an exact upstream model
build, a fresh account context, or independent acceptance.

## Adopted advice

- State explicitly that duplicate optional detail fields fail closed.
- Give `ALT` a closed grammar and permit it only with `not_preferred`.
- Preserve parser independence from Codex lifecycle and stop-hook delivery.
- Include negative fixtures for unknown operators and fields, duplicate fields,
  missing required fields, oversize values, ambiguous delimiters, and
  control-like extensions.

## Corrected or rejected advice

- The suggested dictionary shape is a useful field inventory but omits the
  protocol/schema version and record kind needed for stable machine use.
- The response repeated all preference alternatives rather than selecting one.
  Under the controlling contract its attributed preference is `not_stated`.
  No preference is inferred from its generally constructive review.

## Authority ceiling

This response is advisory design input only. Local deterministic tests and the
Dream House coordinator remain the acceptance boundary.
