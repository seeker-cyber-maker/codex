# Intake: TERM compatibility preflight v1

## Objective

Freeze a deterministic, synthetic, offline-only compatibility preflight for
`TERM_NOTATION/1` before any model output is collected or any Dream House
control-plane surface is connected.

## Authority and non-goals

The user authorized continued Dream House work after the standalone TERM syntax
milestone. This run may add source, fixtures, tests, and durable workflow
records only.

It must not call a provider or model, render a model prompt, modify a task,
relay a message, grant authority, alter global hooks, inspect credentials, or
bind an actual model roster.

## Source baseline

- Starting commit: `b434e2a5d6c4cba73d2d21f0cf80373dc8f7cb99`
- Existing unrelated work is preserved: `house/README.md` and
  `house/LOCAL_ZOOKEEPER_CHAT_WORK_WRANGLER_SPEC.md`.
- Inputs are indexed in `SOURCE_INDEX.json` and treated as design evidence, not
  execution authority.
