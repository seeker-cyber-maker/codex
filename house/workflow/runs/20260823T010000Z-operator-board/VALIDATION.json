{
  "schema": "codex-house-validation/1",
  "state": "PASS",
  "scope": "frozen caller-supplied Dream House operator board",
  "checks": [
    {
      "name": "board, view, snapshot, inventory, and CLI focused tests",
      "command": "python3 -m unittest house.relay.tests.test_operator_board house.relay.tests.test_operator_inventory_view house.relay.tests.test_operator_snapshot house.relay.tests.test_snapshot_inventory house.relay.tests.test_cli",
      "result": "13 passed",
      "exit_code": 0
    },
    {
      "name": "all component House test suites",
      "command": "for test_dir in $(find house -type d -name tests -print | sort); do python3 -m unittest discover -s \"$test_dir\" -p 'test_*.py'; done",
      "result": "10 suites / 247 tests passed",
      "exit_code": 0
    },
    {
      "name": "static checks",
      "command": "python3 -m compileall -q house/relay && ruff check changed files && ruff format --check changed files && git diff --check",
      "result": "pass",
      "exit_code": 0
    }
  ],
  "claim_ceiling": "An inert renderer that composes two caller-supplied local static documents. It does not run inventory, read storage, discover/refresh sources, write/repair/delete data, apply retention, read live task/relay state, bind a listener, start a viewer, launch a browser, call iTerm, accept terminal input, create/mutate/dispatch tasks, contact workers/providers, grant authority, or open a reverse channel."
}
