# Dream House parking handoff

The active downstream branch is `codex/dream-house-auto-switcher` at source
commit `31cc8e88bba75f02b4ba2190ec22cf82781f5533`.  The private off-site
backup must resolve the same commit before work resumes.

The source delta after the prior mock-only admission commit consists of:

- a refusing human-authority interface and its focused tests;
- read-only USB enumeration and comparison helpers; and
- the council/design records for the human-authority interface.

No configured Codex runtime, provider request, task dispatch, model selection,
or result admission was performed.  The next live-execution phase remains
blocked on the separately specified runtime qualification and authenticated
single-use human authority gate.
