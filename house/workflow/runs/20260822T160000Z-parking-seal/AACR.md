# Parking review

The continuation boundary is now explicit: the complete committed source delta
is backed up privately, its scoped tests and static checks pass, and its source
tree is recorded.  This is a parking checkpoint, not a promotion.  Future work
must start from the sealed source commit and re-qualify any real runtime path.
