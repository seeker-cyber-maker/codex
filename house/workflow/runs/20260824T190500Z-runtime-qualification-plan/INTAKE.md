# Intake: runtime qualification v2 recovery plan

Objective: determine the smallest no-dispatch path from the sealed source-only
canary entrypoints to a future runtime-profile qualification verifier.

This is a planning and evidence run only. It may read current local source and
the existing prepared MCU operation. It must not create an operation, discover
or copy credentials, build a candidate bundle, sign, launch, contact a provider,
or consume an authority token.

Acceptance: a council-reviewed plan either identifies a complete independent
profile-input set and a new no-dispatch operation-record slice, or records the
specific missing input as a blocker. No result can claim runtime qualification.
