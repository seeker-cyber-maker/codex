# Operation v2 council — plan v1

## Classification and routing advisory

- Project: existing Dream House repository.
- Recovery disposition: resume from the sealed qualification blocker at
  `799adf8d5d`.
- Case type: `systems_critical` design review; no implementation.
- Recommendation: Sol / high for the root synthesis.
- Council lanes: authority-boundary profile across ClinePass, OpenCode Go, and
  explicit OpenRouter free models.
- Reassess: after the council disposition, before any source change.

## Objective

Obtain blind outside review of an immutable v2 operation-preparation proposal
that separates task routing, runtime selection, and dispatch authority while
closing ambient Codex configuration surfaces.

## Graph

1. Freeze proposal and evidence packet.
2. Validate packet and live catalogs with a no-dispatch dry run.
3. Dispatch the same packet independently to three provider lanes.
4. Preserve every response and synthesize a claim ledger.
5. Record one root disposition: accept, bounded revision, one test, or
   `NEEDS_REVIEW`.

## Authority and budget

- Cloud review is allowed for this `cloud-ok`, credential-free packet.
- Use existing free/subscription provider lanes only; no purchase or provider
  configuration change.
- Reviewers are read-only and advisory. They may not modify the repository,
  dispatch a worker, grant authority, or contact another reviewer.
- No Codex worker run, operation/controller mutation, credential projection,
  runtime-profile creation, or hardware action.
- One attempt per lane plus the runner's explicit, profile-pinned transport
  fallbacks. No open-ended retries or round two unless a decision-bearing
  disagreement requires it.

## Acceptance

- All reviewers receive and, when possible, echo the same packet hash.
- All attempts and model/provider identities are retained.
- Synthesis distinguishes evidence from inference and names the smallest safe
  implementation slice or a genuine blocker.
