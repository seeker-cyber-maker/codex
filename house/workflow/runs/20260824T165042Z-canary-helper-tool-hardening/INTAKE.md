# Intake: canary-helper bounded-tool hardening

## Objective

Close the two non-blocking robustness debts preserved by the accepted
pre-signing council: enforce finite subprocess timeouts and make the codec test
runner reserve and clean its own private no-follow output namespace.

## Recovery disposition

`RESUME_FROM_HANDOFF`

The repository was clean at
`29eac0f7d345188f5e3d23242fa4cada077da376`. The predecessor final seal
`76fb5fdea113277608b573d2df101f46d4a036f7d2aa4972c3015c789c85cb1e`
reverified with zero mismatches, and the focused baseline passed 16 tests.

## Authority

The user's `.` continues the accepted Dream House ladder through this smallest
source-only remediation. It permits source edits, deterministic unit tests,
static `/usr/bin/codesign` inspection, and disposable codec-only compilation
and execution. It does not permit parent/helper linking or launch, certificate
discovery, Keychain access, identity signing, network/providers, YubiKey,
generated canaries, or real secrets.

## Claim ceiling

This run may establish only that the local validation tools enforce declared
timeouts and that the codec runner owns a private temporary output lifecycle
under ordinary same-user operation. It does not establish hostile-host
resistance, signed-candidate identity, App Sandbox behavior, process
containment, or secret safety.
