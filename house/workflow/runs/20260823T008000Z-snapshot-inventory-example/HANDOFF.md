# Snapshot inventory example — handoff

The inventory command now has a copyable JSON template at
`house/relay/examples/snapshot-inventory-paths.example.json`. Replace its
nonexistent placeholders with real absolute envelope paths before invoking the
command. No path is inferred or created. The JSON template/help check and all
10 component suites (243 tests) pass.

Next candidate: decide whether to remain at this sealed offline foundation or
start a separately scoped static operator presentation layer. The latter would
need a new plan because it changes the human-facing surface, but must still
remain read-only and inactive by default.
