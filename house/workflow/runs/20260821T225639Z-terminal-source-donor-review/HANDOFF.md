# Handoff

## Current

Official GitHub source review is complete. No applications were launched and no
runtime or configuration state was changed.

## Confirmed

- Warp: snapshot plus ordered event replay and duplicate-replay suppression.
- Wave: stable controller IDs and serialized reconciliation.
- iTerm2: source-backed integration-health checks; companion sharding remains a
  design draft.
- Ghostty and kitty: benchmark boundaries expose why the prior terminal results
  cannot rank rendering performance.
- Kitty: versioned, cancellable remote-control envelope patterns.

## Next

Fold only the eight-item recommended synthesis from `SOURCE_FINDINGS.md` into
the Dream House architecture/specification. Do not implement terminal or cloud
relay features until that contract has an offline replay/reconciliation test.

