# Review: agy-gemini-pro-cross-project

Packet SHA-256: 7e3fb6397edc8b04ee9a4783033f450d331dfc76cacbcd48a596c3e20acbaf8d
Dispatch model/provider: gemini-3.1-pro-low / Google Antigravity
Reviewer self-report: gemini-3.1-pro-low
Harness: provider-orchestration Antigravity text-only OpenAI shim
System-prompt profile: council role only; otherwise unknown
Memory: disabled
Reasoning mode: unknown
Disposition: completed

## Verdict
revise, selecting separated model-native dialects behind a canonical semantic layer. The evidence confirms current models already require distinct structural dialects (e.g., Claude XML, Gemini JSON) for optimal friction-reducing translation at the boundary.

## Direct observations
- The Contractor Station proof of concept already translates between one canonical core and provider-facing `canonical-json`, `gptoss-harmony`, `gemini-json`, and `claude-xml` representations (Directly established boundaries, #6; Known Agent Registers, #7.2).
- Anthropic research reports "materially different coordination behavior across model generations" even within a single lineage (Known Agent Registers, #6).
- A2A suggestion box contract proposes retaining the "model-native response" and translating it at the boundary rather than forcing a unified input (A2A Suggestion Box Contract, #1).

## Inferences
- Claim: Forcing a single united NAR language across mixed lineages will increase error rates and repair burdens compared to allowing model-native dialects. 
- Confidence: High. 
- Falsifier: A mixed-lineage evaluation (e.g., Claude and Gemini) showing lower latency, token cost, and error rates when forced to use a unified text NAR dictionary compared to communicating via their native, dialect-adapted structures.

## Lexicon corrections
- Downgrade "OpenAI incident operational register" from a generalized natural language baseline to an environment-specific transport adaptation, as the directory-name protocol was a constraint response rather than a pure linguistic emergence.

## Suggestion
- Target: `USER_DECISION_ADDENDUM.md` Fork Decision
- Proposal: Standardize the A2A architecture on separated model-native dialects (Option 2), leveraging the existing Contractor Station adapter pattern to translate disparate native outputs (XML, Harmony, etc.) into the canonical Dream House deterministic relay.
- Benefit: Prevents performance degradation caused by forcing models out of their heavily pretrained syntactic formats while strictly isolating authority and semantic state in the canonical core.
- Risk: Translator layers must be rigorously maintained to prevent syntax-smuggling attacks from becoming canonical commands.

## Unsupported or contradicted claims
- Claim: "Progressive natural compression reduces A2A token and latency cost without increasing semantic error." (RFC Hypotheses, 49.1)
- Reason: The Meta/Facebook negotiation experiment explicitly showed that unconstrained task-optimized communication diverges from interpretable language, increasing semantic error (misinterpretation) outside of the original context generation. 

## Recommendation
Execute a controlled task assignment and verification test between a Gemini model and a Claude model. Compare two conditions: Condition A forces both models to use a single united text NAR register. Condition B allows both to use their native dialect adapters via the Contractor Station. Measure task completion rate and false capability acceptance.

## Limitations
- The primary OpenAI incident artifact (raw message board corpus) is not provided, making it impossible to evaluate if their spontaneous register heavily utilized environment-specific codebooks that would fail entirely in a clean-room transfer test.
